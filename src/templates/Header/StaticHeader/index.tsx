'use client';

import { IHeadersLayoutProps } from '@/@types/Header';

export default function StaticHeader({ t, onClick }: IHeadersLayoutProps) {
  return <div style={{ margin: '30px 0' }}>Header</div>;
}
